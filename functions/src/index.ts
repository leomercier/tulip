/**
 * Firebase Cloud Functions — retired.
 *
 * All API routes have been migrated to the Next.js app (apps/web/src/app/api/)
 * and are served via Cloud Run. This package is kept as a shell so
 * `firebase emulators:start --only firestore` continues to work for local dev.
 */
